# includes

```
문자열에 인수로 전달하는 문자열이 포함되어 있는지 확인한다
존재한다면 true 를, 없다면 false를 반환한다.
해당 메소드는 대소문자를 구분한다.
```

```js
let str = 'Javascript';
console.log(str.includes('Java')); //true
console.log(str.includes('java')); //false
```
