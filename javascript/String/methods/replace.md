# replace

```
문자열에서 포함하고 있는 문자열 대신 새로운 문자열로 대체한다.

string.replace(바꾸려는 문자열, 대체할 문자열)
```

```js
let str = 'it is method of replace in js';

console.log(str.replace('method', 'methods')); // it is methods of replace in js;
```
